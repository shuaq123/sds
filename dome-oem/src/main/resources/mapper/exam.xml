<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="oem.example.mapper.studentExamMapper">

    <select id="studentexamlist" resultType="com.example.modle.studentExam">
        SELECT examId,coverTitle,examName,particulars,startTime,endTime,realNameRegistration,simId,isSroce,examTime,creatorTime
        FROM student_exam
    </select>

</mapper>